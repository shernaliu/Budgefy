<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"

    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    tools:context="com.mapp.budgefy.MainActivity">

    <!--
    Notes:

    I have created "divider.xml" in drawable folder so that you use this divider in the ListView
    to segregate 1 row from another

    android:divider="@drawable/divider" -> Use the divider.xml I have created

    android:dividerHeight="2dip" -> Sets the space of 1 "row" in the ListView


    -->
    <ListView
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:divider="@drawable/divider"
        android:dividerHeight="2dip"
        android:id="@+id/lvReceiptRecords" />

</RelativeLayout>
